$(".modal").modal('hide');
$("body").append("<%= j render('show', app: @app ) %>");
$("#app_<%= @app.name %>_control_panel_modal").modal("show");
$("#app_<%= @app.name %>_control_panel_modal").on("hidden.bs.modal", function(){$(this).remove();});
