$(".modal").modal('hide');
$("body").append("<%= j render('show', app: @app ) %>");
$("#app_<%= @app.id %>_report_modal").modal("show");
$("#app_<%= @app.id %>_report_modal").on("hidden.bs.modal", function(){$(this).remove();});
